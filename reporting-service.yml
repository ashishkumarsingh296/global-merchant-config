server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/gmps_reporting
    username: root
    password: root

 application:
    name: reporting-service

  config:
    import: "optional:configserver:http://config-server:8888"

