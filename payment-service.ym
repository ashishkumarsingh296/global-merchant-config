server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/gmps_payment
    username: root
    password: root

application:
    name: payment-service

  config:
    import: "optional:configserver:http://config-server:8888"


  kafka:
    bootstrap-servers: kafka:9092
