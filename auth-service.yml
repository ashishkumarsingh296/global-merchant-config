server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/gmps_auth
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

  application:
    name: auth-service

  config:
    import: "optional:configserver:http://config-server:8888"

