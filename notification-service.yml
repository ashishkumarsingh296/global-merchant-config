server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/gmps_notification
    username: root
    password: root

  application:
    name: notification-service

  config:
    import: "optional:configserver:http://config-server:8888"


  kafka:
    bootstrap-servers: kafka:9092
